/* exported getValue */
// make storage for the value
// find the value at given position and store in value storage
// give back location of storage consisting of value.
function getValue(object, key) {
  var keyValue = '';
  keyValue = object[key];
  return keyValue;
}
