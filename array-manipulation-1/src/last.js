/* exported last */
// make storage for the last element of array
// get last element of array and store it
// give back location of storage for last element
function last(array) {
  var lastIndex = array[array.length - 1];
  return lastIndex;
}
